<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 min-h-screen px-8 py-8">

    <a href="/read" 
       class="mb-6 px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-800 transition duration-200">
        ← BACK
    </a>

    <h1 class="text-3xl tracking-tighter text-white text-center my-6 underline decoration-white">
        Update User Page
    </h1>

    <div>
        <form action="/update/<%= user._id %>" method="post" class="mx-6">
            <input type="text" name="name" value="<%= user.name %>"
                class="h-10 w-full bg-zinc-800 mt-4 text-white px-4 py-2 rounded-lg outline-none 
                       focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                placeholder="Name">

            <input type="email" name="email" value="<%= user.email %>"
                class="h-10 w-full bg-zinc-800 mt-4 text-white px-4 py-2 rounded-lg outline-none 
                       focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                placeholder="Email">

            <input type="text" name="imageUrl" value="<%= user.imageUrl %>"
                class="h-10 w-full bg-zinc-800 mt-4 text-white px-4 py-2 rounded-lg outline-none 
                       focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                placeholder="Image URL">

            <button type="submit" 
                class="mt-4 px-4 py-2 bg-green-500 text-white rounded hover:bg-green-800 cursor-pointer">
                Update
            </button>
        </form>
    </div>
</body>
</html>
